apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-ngw
  namespace: nextgis
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 2Gi
  storageClassName: ngw-gera-storage 
  